상속 => 재사용
건물<----학교<----고등학교
개념적인 크다, 작다
건물이 제일 크다
위로 갈수록 일반화, 아래로 갈수록 세분화
위의 클래스: 부모클래스 super class
아래의 클래스: 자식클래스 sub class


Car 정의: 멤버변수, 멤버메서드 2개씩
Truck 정의: 변수, 메서드 1개 추가
coffeetruck: 변수, 메서드 1개 추가

Caffe - main

클래스다이어그램 그리기

자식클래스 객체생성시 부모클래스의 기본생성자를 호출한다
(부모클래스가 먼저 생성되어야 자식클래스를 만들 수 있으므로)

Man m = new Man();
	Person() 생성자가 먼저 호출되고, 여기에 추가로 Man() 생성자가 실행된다.
	 