<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="member">
<insert parameterType="memberVO" id="insert">insert into "member" values (#{id}, #{pw}, #{name}, #{tel}) </insert>
<delete parameterType="memberVO" id="delete">delete from "member" where id = #{id} </delete>
<update parameterType="memberVO" id="update">update "member" set tel = #{tel} where id = #{id} </update>
</mapper>